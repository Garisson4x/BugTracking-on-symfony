{% extends 'base.html.twig' %}

{% block title %}New Tickets{% endblock %}

{% block body %}
    <h1>Create new Tickets</h1>

    {{ include('tickets/_form.html.twig') }}

    <a href="{{ path('projects_show', {'id': project_id}) }}">back to list</a>
{% endblock %}
