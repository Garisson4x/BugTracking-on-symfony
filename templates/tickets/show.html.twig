{% extends 'base.html.twig' %}

{% block title %}Tickets{% endblock %}

{% block body %}
    <h1>Ticket</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ ticket.title }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ ticket.type }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ ticket.status }}</td>
            </tr>
            <tr>
                <th>Desription</th>
                <td>{{ ticket.desription }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ ticket.file }}</td>
            </tr>
            <tr>
                <th>File_name</th>
                <td>{{ ticket.fileName }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ ticket.createdAt ? ticket.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ ticket.updatedAt ? ticket.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projects_show', {'id': ticket.project.id}) }}">back to project</a>

    <a href="{{ path('tickets_edit', {'project_id': ticket.project.id, 'id': ticket.id}) }}">edit</a>

    {{ include('tickets/_delete_form.html.twig') }}

    <h1>Comments</h1>
      {% for comment in comments %}
        <div>Author: {{ comment.author }}</div>
        <div> {{ comment.context }} </div>
      {% endfor %}
    {{ render(controller('App\\Controller\\CommentsController::new', {'ticket_id': ticket.id}))}}
{% endblock %}
